import React from 'react';
import {connect} from 'react-redux';

class MakeMarketPublicPage extends React.Component {
    render() {
        return(
            <div>
                MakeMarketPublicPage
            </div>
        )
    }
}

export default connect()(MakeMarketPublicPage);